{
  "description": "tale-mode autonomous goal loop (Layer 1). Deterministic Stop gate (bash): keeps the turn going until an armed .claude/active-goal.json check passes; silent (and free) when no goal is armed. Bounded by the goal-file's max_rounds + a fail-open; no platform dependency. The optional Layer-2 Sonnet governor ships as a separate companion plugin: tale-mode-governor.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/stop-goal-loop.sh\"",
            "timeout": 600
          }
        ]
      }
    ]
  }
}
